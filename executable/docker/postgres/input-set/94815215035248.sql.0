
  CREATE TABLE invoice (
    year int,
    invoice_number int,
    PRIMARY KEY (year, invoice_number)
  );

  INSERT INTO invoice VALUES (2016, 1), (2016, 2);

;
  DROP TABLE invoice;

;
  CREATE TABLE invoice (
    year int,
    invoice_number int,
    PRIMARY KEY (year, invoice_number)
  );

  INSERT INTO invoice VALUES (2016, 1), (2016, 2);

;
  DROP TABLE invoice;

;
  CREATE TABLE invoice (
    year int,
    invoice_number int,
    PRIMARY KEY (year, invoice_number)
  );

  INSERT INTO invoice VALUES (2016, 1), (2016, 2);

;
  DROP TABLE invoice;

;