
    SET default_transaction_isolation = 'read committed';

;
    SET default_transaction_isolation = 'read committed';

;
    SET default_transaction_isolation = 'read committed';

;
    SET default_transaction_isolation = 'read committed';

;
    SET default_transaction_isolation = 'read committed';

;