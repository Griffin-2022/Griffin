SELECT count(1) > 0
FROM pg_stat_subscription_workers
WHERE last_error_relid = 'test_tab1'::regclass
    AND starts_with(last_error_message, 'duplicate key value violates unique constraint') AND subrelid IS NULL AND last_error_command = 'INSERT' AND last_error_xid = '720'::xid
;