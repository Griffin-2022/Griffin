
    SET default_transaction_isolation = 'read committed';
    SET spec.session = 2;

;
    SET default_transaction_isolation = 'read committed';
    SET spec.session = 2;

;
    SET default_transaction_isolation = 'read committed';
    SET spec.session = 2;

;
    SET default_transaction_isolation = 'read committed';
    SET spec.session = 2;

;
    SET default_transaction_isolation = 'read committed';
    SET spec.session = 2;

;