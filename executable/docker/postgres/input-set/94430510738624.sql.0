SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;SELECT pg_advisory_lock(0);
;