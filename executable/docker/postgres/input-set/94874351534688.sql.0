INSERT INTO t SELECT * FROM generate_series(1, 3000);
;INSERT INTO t2 SELECT * FROM generate_series(1, 3000);
;CREATE PUBLICATION testpub FOR TABLE t;
;SELECT pg_create_logical_replication_slot('testslot', 'pgoutput');
;