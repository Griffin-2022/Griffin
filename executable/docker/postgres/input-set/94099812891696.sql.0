
    SET default_transaction_isolation = 'read committed';
    SET spec.session = 1;

;
    SET default_transaction_isolation = 'read committed';
    SET spec.session = 1;

;
    SET default_transaction_isolation = 'read committed';
    SET spec.session = 1;

;
    SET default_transaction_isolation = 'read committed';
    SET spec.session = 1;

;
    SET default_transaction_isolation = 'read committed';
    SET spec.session = 1;

;