
  CREATE TABLE ints (key int, val text, PRIMARY KEY (key) INCLUDE (val));

;
  DROP TABLE ints;

;
  CREATE TABLE ints (key int, val text, PRIMARY KEY (key) INCLUDE (val));

;
  DROP TABLE ints;

;
  CREATE TABLE ints (key int, val text, PRIMARY KEY (key) INCLUDE (val));

;
  DROP TABLE ints;

;
  CREATE TABLE ints (key int, val text, PRIMARY KEY (key) INCLUDE (val));

;
  DROP TABLE ints;

;
  CREATE TABLE ints (key int, val text, PRIMARY KEY (key) INCLUDE (val));

;
  DROP TABLE ints;

;
  CREATE TABLE ints (key int, val text, PRIMARY KEY (key) INCLUDE (val));

;
  DROP TABLE ints;

;
  CREATE TABLE ints (key int, val text, PRIMARY KEY (key) INCLUDE (val));

;
  DROP TABLE ints;

;
  CREATE TABLE ints (key int, val text, PRIMARY KEY (key) INCLUDE (val));

;
  DROP TABLE ints;

;