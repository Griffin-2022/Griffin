CREATE PROCEDURE consume_xid(cnt int)
AS $$
DECLARE
    i int;
    BEGIN
        FOR i in 1..cnt LOOP
            EXECUTE 'SELECT pg_current_xact_id()';
            COMMIT;
        END LOOP;
    END;
$$
LANGUAGE plpgsql;
;