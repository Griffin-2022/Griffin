
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;
  DROP TABLE IF EXISTS foo;
  CREATE TABLE foo (
	key		int PRIMARY KEY,
	value	int
  );

  INSERT INTO foo VALUES (1, 1);

;
  DROP TABLE foo;

;