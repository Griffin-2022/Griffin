CREATE TABLE elephant(
      name VARCHAR(32), 
      color VARCHAR(16), 
      age INTEGER, 
      UNIQUE(name, color)
    );
;
;CREATE VIRTUAL TABLE pachyderm USING echo(elephant);
;SELECT sql FROM sqlite_schema WHERE type = 'table' AND name = ?
;SELECT * FROM 'elephant'
;PRAGMA index_list(elephant)
;PRAGMA index_info(sqlite_autoindex_elephant_1)
;
;DROP TABLE pachyderm;
;
;SELECT name FROM sqlite_master WHERE type = 'table';
;
;CREATE VIRTUAL TABLE pachyderm USING echo(
;
;CREATE VIRTUAL TABLE pachyderm USING echo(elephant);
;SELECT sql FROM sqlite_schema WHERE type = 'table' AND name = ?
;SELECT * FROM 'elephant'
;PRAGMA index_list(elephant)
;PRAGMA index_info(sqlite_autoindex_elephant_1)
;
;
;
;DROP TABLE pachyderm;
;
;DROP TABLE pachyderm;
;
;DROP TABLE pachyderm;
;
;DROP TABLE pachyderm;
;
;
;