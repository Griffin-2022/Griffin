CREATE TABLE t1 (a PRIMARY KEY);
;INSERT INTO t1 VALUES(1);
;
;SELECT b AS c FROM (
    SELECT a AS b FROM (
      SELECT a FROM t1 WHERE a=1 OR (SELECT sum(a) OVER ())
    ) 
    WHERE b=1 OR b<10
  ) 
  WHERE c=1 OR c>=10;
;
;