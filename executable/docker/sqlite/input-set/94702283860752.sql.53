CREATE TABLE SomeObject (
     a INTEGER PRIMARY KEY,
     x TEXT GENERATED ALWAYS AS(HEX(a)) VIRTUAL
  )
;CREATE TABLE SomeObject (
     a INTEGER PRIMARY KEY,
     x TEXT GENERATED ALWAYS AS(HEX(a)) VIRTUAL
  )
;PRAGMA index_list='SomeObject'
;SELECT count(*) FROM sqlite_schema WHERE name='SomeObject_idx_00000078' AND type in ('index','table','view')
;SELECT count(*) FROM sqlite_schema WHERE name='SomeObject_idx_00000078' AND type in ('index','table','view')
;CREATE INDEX SomeObject_idx_00000078 ON SomeObject(x)
;CREATE INDEX SomeObject_idx_00000078 ON SomeObject(x)
;SELECT max(i.seqno) FROM   sqlite_schema AS s,   pragma_index_list(s.name) AS l,   pragma_index_info(l.name) AS i WHERE s.type = 'table'
;PRAGMA index_list='SomeObject'
;PRAGMA index_info='SomeObject_idx_00000078'
;ANALYZE; PRAGMA writable_schema=1
;ANALYZE;
;SELECT tbl,idx,stat FROM 'main'.sqlite_stat1
;SELECT tbl,idx,stat FROM 'main'.sqlite_stat1
;PRAGMA writable_schema=1
;SELECT s.rowid, s.name, l.name FROM   sqlite_schema AS s,   pragma_index_list(s.name) AS l WHERE s.type = 'table'
;SELECT name, coll FROM pragma_index_xinfo(?) WHERE key
;INSERT INTO sqlite_stat1 VALUES(?, ?, ?)
;PRAGMA index_list='SomeObject'
;PRAGMA index_xinfo='SomeObject_idx_00000078'
;PRAGMA index_list='sqlite_stat1'
;ANALYZE sqlite_schema
;ANALYZE sqlite_schema
;SELECT tbl,idx,stat FROM 'main'.sqlite_stat1
;SELECT tbl,idx,stat FROM 'main'.sqlite_stat1
;EXPLAIN QUERY PLAN 
  SELECT * FROM SomeObject WHERE x=?;
;