CREATE TABLE abc(a INTEGER PRIMARY KEY, x, y);
;CREATE UNIQUE INDEX abc1 ON abc(('x' || x) COLLATE nocase);
;
;INSERT INTO abc VALUES(1, 'one', 'two');
;
;INSERT INTO abc VALUES(2, 'one', NULL) ON CONFLICT DO NOTHING;
;
;INSERT INTO abc VALUES(2, 'one', NULL) ON CONFLICT ('x' || x) DO NOTHING;
;
;INSERT INTO abc VALUES(2, 'one', NULL) ON CONFLICT (('x' || x) COLLATE nocase) DO NOTHING;
;
;INSERT INTO abc VALUES(2, 'one', NULL) ON CONFLICT (('x' || x) COLLATE binary) DO NOTHING;
;INSERT INTO abc VALUES(2, 'one', NULL) ON CONFLICT (x || 'x') DO NOTHING;
;INSERT INTO abc VALUES(2, 'one', NULL) ON CONFLICT ((('x' || x))) DO NOTHING;
;
;SELECT * FROM abc
  
;