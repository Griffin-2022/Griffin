CREATE TABLE abc(a INTEGER PRIMARY KEY, x, y);
;CREATE UNIQUE INDEX abc1 ON abc(x) WHERE y>0;
;CREATE UNIQUE INDEX abc2 ON abc(y) WHERE x='xyz' COLLATE nocase;
;
;INSERT INTO abc VALUES(1, 'one', 1);
;INSERT INTO abc VALUES(2, 'two', 2);
;INSERT INTO abc VALUES(3, 'xyz', 3);
;INSERT INTO abc VALUES(4, 'XYZ', 4);
;
;INSERT INTO abc VALUES(5, 'one', 10) ON CONFLICT DO NOTHING
    
;INSERT INTO abc VALUES(5, 'one', 10) ON CONFLICT(x) WHERE y>0 DO NOTHING
    
;INSERT INTO abc VALUES(5, 'one', 10) ON CONFLICT(x) DO NOTHING
    
;INSERT INTO abc VALUES(5, 'one', 10) ON CONFLICT(x) WHERE y>=0 DO NOTHING
    
;INSERT INTO abc VALUES(5, 'one', 10) ON CONFLICT(y) WHERE x='xyz' COLLATE nocase DO NOTHING
    
;SELECT * FROM abc
  
;INSERT INTO abc VALUES(5, 'xYz', 3) ON CONFLICT DO NOTHING
    
;INSERT INTO abc VALUES(5, 'xYz', 3) ON CONFLICT(y) WHERE x='xyz' COLLATE nocase DO NOTHING
    
;INSERT INTO abc VALUES(5, 'xYz', 3) ON CONFLICT(y) WHERE x='xyz' COLLATE binary DO NOTHING
    
;INSERT INTO abc VALUES(5, 'xYz', 3) ON CONFLICT(x) WHERE y>0 DO NOTHING
    
;CREATE TABLE w1(a INT PRIMARY KEY, x, y);
;CREATE UNIQUE INDEX w1expr ON w1(('x' || x));
;INSERT INTO w1 VALUES(2, 'one', NULL)
    ON CONFLICT (('x' || x) COLLATE nocase) DO NOTHING;
;