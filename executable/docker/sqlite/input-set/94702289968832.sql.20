CREATE TABLE t1(window, y integer);
;INSERT INTO t1 VALUES(1, 'a');
;INSERT INTO t1 VALUES(2, 'b');
;INSERT INTO t1 VALUES(3, 'c');
;INSERT INTO t1 VALUES(4, 'd');
;INSERT INTO t1 VALUES(5, 'e');
;
;SELECT group_concat(window, '.') OVER (ORDER BY y) FROM t1
  
;SELECT sum(window) OVER w FROM t1 WINDOW w AS (ORDER BY y)
  
;SELECT sum(alias.window) OVER w FROM t1 alias WINDOW w AS (ORDER BY y)
  
;SELECT sum(window) alias FROM t1
  
;SELECT window('hello world');
;
;CREATE TABLE window(x COLLATE window);
;INSERT INTO window VALUES('bob'), ('alice'), ('cate');
;SELECT * FROM window ORDER BY x COLLATE window;
;
;DROP TABLE window;
;CREATE TABLE x1(x);
;INSERT INTO x1 VALUES('bob'), ('alice'), ('cate');
;CREATE INDEX window ON x1(x COLLATE window);
;SELECT * FROM x1 ORDER BY x COLLATE window;
;
;CREATE TABLE t4(x, y);
;
;SELECT * FROM t4 window, t4;
;
;