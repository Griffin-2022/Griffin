CREATE VIRTUAL TABLE 't8' USING expert('CREATE TABLE x(''a'' COLLATE BINARY, ''b'' COLLATE BINARY)')
;CREATE VIRTUAL TABLE 't8' USING expert('CREATE TABLE x(''a'' COLLATE BINARY, ''b'' COLLATE BINARY)')
;
  SELECT * FROM t8 WHERE rowid=?

;